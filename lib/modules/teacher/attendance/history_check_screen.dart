import 'package:flutter/material.dart';

class HistoryCheckScreen extends StatelessWidget {
  const HistoryCheckScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('History Check')),
    body: const Center(child: Text('Check attendance history placeholder')),
  );
}
