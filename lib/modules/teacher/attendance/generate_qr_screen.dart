import 'package:flutter/material.dart';

class GenerateQrScreen extends StatelessWidget {
  const GenerateQrScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Generate QR')),
    body: const Center(child: Text('Generate dynamic QR placeholder')),
  );
}
