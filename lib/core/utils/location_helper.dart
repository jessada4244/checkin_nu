// Minimal location helper (placeholder)
import 'dart:async';

class LocationHelper {
  /// Return a pair of latitude/longitude. Replace with real implementation.
  static Future<Map<String, double>> getCurrentLocation() async {
    return Future.value({'lat': 0.0, 'lng': 0.0});
  }
}
