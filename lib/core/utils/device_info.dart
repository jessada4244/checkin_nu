// Minimal device info helper (placeholder)
import 'dart:async';

class DeviceInfo {
  /// Return a device identifier. Replace with real implementation.
  static Future<String> getDeviceId() async {
    return Future.value('unknown-device-id');
  }
}
